binaries
--------
    callsign-lookupd:
            * QRZ
                    * Login: WORKING
                    * Callsign Lookup: WORKING
		    * Commandline lookups (./lookup CALLSIGN): WORKING
	    * Cache: WIP

    decoderd-ft8:
            * some example code, not yet started really

    encoderd-ft8:
            * may reuse example code for some

    flac-streamerd:
            * placeholder, NYI

    ft8goblin:
            * TUI
		    * InputBox completed
                    * TextArea: widget needs finished

    sigcapd:
            * placeholder, NYI
